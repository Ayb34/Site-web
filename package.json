{
  "name": "heritage-musulman",
  "version": "1.0.0",
  "scripts": {
    "compile": "esbuild src/app.jsx --minify --target=es2017 --jsx=transform --outfile=app.js",
    "watch": "esbuild src/app.jsx --minify --target=es2017 --jsx=transform --outfile=app.js --watch"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "stripe": "^17.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
